# CLAUDE.md

This repository's agent guidance is consolidated in [`AGENTS.md`](AGENTS.md). That file is agent-agnostic and is the single source of truth — read it directly.

Maintained as a stub so Claude Code's auto-loader finds something at this path.
